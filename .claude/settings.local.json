{
  "permissions": {
    "allow": [
      "Bash(pip install *)",
      "Bash(python -c \"import gradio, groq, sklearn, pandas, numpy, matplotlib, seaborn, joblib; print\\('All imports OK'\\)\")",
      "Bash(python -m src.preprocess)"
    ]
  }
}
